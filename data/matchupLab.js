import {calculateDamage,getStats,getEffectiveSpeed} from './damageCalc'; import {getFactorySets} from './setIdentity'; const setsFor=p=>p?.sets||getFactorySets(p?.species||p?.name)||[];
export function analyzeSetPair({attacker,defender,attackerSet,defenderSet,level=50,round=1,weather='none',attackerStatus='healthy',defenderStatus='healthy',attackerStages={},defenderStages={}}={}){if(!attacker||!defender||!attackerSet||!defenderSet)return null;const moves=(attackerSet.moves||[]).map(m=>typeof m==='string'?m:m?.name).filter(Boolean);const moveResults=moves.map(moveName=>({moveName,damage:calculateDamage({attacker,defender,attackerSet,defenderSet,level,round,moveName,weather,attackerStatus,defenderStatus,attackerStages,defenderStages})}));const a=getEffectiveSpeed(getStats(attacker,attackerSet,level,round),attackerStatus),d=getEffectiveSpeed(getStats(defender,defenderSet,level,round),defenderStatus);return {attacker,defender,attackerSet,defenderSet,attackerSpeed:a,defenderSpeed:d,speedRelation:a>d?'outspeeds':a<d?'is slower':'speed ties',moveResults};}
export function analyzeAllSets({attacker,defender,level=50,round=1,weather='none'}={}){const rows=[];for(const a of setsFor(attacker))for(const d of setsFor(defender))rows.push(analyzeSetPair({attacker,defender,attackerSet:a,defenderSet:d,level,round,weather}));return rows;}
export function summarizeMatchup(rows=[]){const valid=rows.filter(Boolean),moves=valid.flatMap(r=>r.moveResults).map(x=>x.damage).filter(Boolean);return {pairs:valid.length,fasterPairs:valid.filter(r=>r.speedRelation==='outspeeds').length,slowerPairs:valid.filter(r=>r.speedRelation==='is slower').length,ties:valid.filter(r=>r.speedRelation==='speed ties').length,bestPercentMax:moves.reduce((a,b)=>Math.max(a,b.percentMax||0),0),bestPercentMin:moves.reduce((a,b)=>Math.max(a,b.percentMin||0),0)};}